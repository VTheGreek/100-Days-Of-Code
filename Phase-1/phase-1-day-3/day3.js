// Create a function greetUser that takes two parameters:
// - A name (string)
// - A callback function
// The greetUser function should call the callback and pass the name to it.
// Test it with a callback that prints "Hello, <name>!".

// function greetUser(name, callback) {
//     callback(name);  
// }

// function sayHello(userName) {
//     console.log(`Hello, ${userName}!`);
// }

// greetUser("G", sayHello);



// Create a function processName that takes:
// - A name (string)
// - A callback function
// processName should call the callback and pass the name to it.
// - Test it with a callback that:
// - Converts the name to uppercase
// Prints the result

// function processName(name, callback) {
//     callback(name);
// }

// function upperCase(name) {
//     console.log(name.toUpperCase());
// }

// processName("vas", upperCase)



// Create a function nameLength that takes:
// - A name (string)
// - A callback function
// nameLength should call the callback and pass the length of the name.
// Test it with a callback that prints the length.

// function nameLength(name, callback) {
//     callback(name);
// }

// function result(name) {
//     console.log(name.length)
// }

// nameLength("vas", result)